import { SocketAuthMiddleware } from './socket-auth.middleware';

describe('SocketAuthMiddleware', () => {
  it('should be defined', () => {
    // expect(new SocketAuthMiddleware()).toBeDefined();
  });
});
